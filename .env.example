# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/nayan-db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=root

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_that_is_at_least_256_bits_long

# Encryption at Rest Secret Key (Must be EXACTLY 16, 24, or 32 bytes for AES)
ENCRYPTION_KEY=MySuperSecretEncryptionKey123456
OLD_ENCRYPTION_KEY=previous_key_for_rotation_if_any

# Backup Configuration (Phase 8)
BACKUP_ENCRYPTION_PASS=SuperSecretBackupKey2026!
